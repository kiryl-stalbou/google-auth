name: google_auth

publish_to: none

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    
  crypto: ^3.0.3
  url_launcher: ^6.3.0

dev_dependencies:
  lints: ^3.0.0
